  <% chats.each do |chat| %>
    <% if chat.member_id == current_member.id %>
      <p class="chat-sender">
        <%= chat.message %>
        <%= link_to member_path(chat.member) do %>
          <%= attachment_image_tag chat.member , :profile_image, :fill, 30, 30, fallback: "no_image.jpg", size:"30x30" %>
        <% end %>
      </p>
    <% else %>
      <p class="chat-receiver">
        <%= link_to member_path(chat.member)  do %>
          <%= attachment_image_tag chat.member , :profile_image, :fill, 30, 30, fallback: "no_image.jpg", size:"30x30" %>
        <% end %>
        <%= chat.message %>
    <% end %>
  <% end %>